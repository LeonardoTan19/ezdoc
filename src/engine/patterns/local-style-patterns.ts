export const LOCAL_STYLE_TARGET_PATH_PATTERN = /^[a-zA-Z_][\w]*(\.[a-zA-Z_][\w]*)+$/

export const LOCAL_STYLE_SCOPE_PREFIX = 'content.'

export const UNSAFE_PATH_SEGMENT_SET = new Set(['__proto__', 'prototype', 'constructor'])
